[project]
name = "cognito-csv-exporter"
version = "1.0.0"
description = "Export Amazon Cognito User Pool records into CSV"
readme = "README.md"
requires-python = ">=3.7"
dependencies = ["boto3>=1.12.42", "botocore>=1.15.42", "colorama>=0.4.3"]

[tool.uv]
dev-dependencies = []
